---
"design-system": minor
---

Add Button, Card, Header, Page, and HomePage components, and wire them to shared design tokens (`src/tokens.json`, DTCG format) for color, radius, shadow, and font size instead of hardcoded values.
